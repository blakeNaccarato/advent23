[pytest]
addopts =
    --strict-config
    --strict-markers
    --verbose
    --color yes
    -p no:legacypaths
    -r a
aoc_year = 2023
cache_dir = .cache/.pytest_cache
markers = slow
testpaths = tests
xfail_strict = True
